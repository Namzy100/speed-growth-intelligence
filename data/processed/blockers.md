# Blockers & watch items

Maintained by hand. The morning brief (pipelines/morning_brief.py) surfaces
`- [BLOCKED]` lines as alerts and `- [WATCH]` lines as routine watch items.
Delete a line when it's resolved.

- [WATCH] Handover batch on `feat/handover-batch` (gh-pages deploy + morning brief + merchant vertical expansion + segment rebuild + strategy rebuild + Pipeline-Board card UX) is pushed to TrySpeed/Marketing-Agent but not merged — Jay reviews, merges, and raises the PR from `main` himself. After it lands: point GitHub Pages at `gh-pages`/root (public) and let a daily-sync run seed that branch.
- [WATCH] Segmentation: rebuild DONE + live (2026-07-22); held-back set CLOSED OUT (2026-07-22). `description` column added + persisted (`database.py`, all fetchers: YouTube snippet.description / TikTok signature / X bio). Of the 41 held-back, 35 got a real description via name-based re-fetch and were re-judged with real content + written (`creators/backfill_description.py` + `creators/rejudge_heldback.py`) — nearly all confirmed → `general` (real bios proved they were keyword false-positives: tech reviewers, expat/travel vlogs, comedy, anti-gambling). **4 stay PERMANENTLY parked** (no fix implied): ruchi kokcha (X) + Terrian, Cassie, Ethan Zohn (TikTok) — name-based re-fetch found no match and the DB stores no handle/URL, so there's no reliable path to their bio. (Was 6: Kevin O'Shea and Harley XVI are no longer in the DB — removed/renamed in an earlier cleanup — so the count is corrected to 4 (verified against Supabase 2026-07-23).)
- [WATCH] Instagram discovery hashtag set skews toward business/exchange accounts, not individual creators (low priority, not blocking). The current `SEGMENTS` hashtags in `creators/instagram_discovery.py` (e.g. #moneytransfer, #cryptowallet, #cryptocasino) surface money-exchange/exchange/casino businesses more than people — the 2026-07-23 write bore this out: **3 of the 4** new creators were brands (BitValve Crypto Exchange, Cryptocasinosonnet.com, FortuneZ), while the 4th (Francis Osei-Amoaten, 121,620 followers) is a genuine individual creator. So the skew is real but not total. Nothing dishonest is happening: the `is_influencer` classifier flags Brand/Media and the floor+scorer filter honestly, brands are just lower-value for creator *partnerships* specifically. A future tuning pass biasing the hashtags toward individual-creator language (e.g. personal-finance/creator-lifestyle tags rather than product/service tags) would likely improve the individual-vs-brand ratio. Tracked so it's not forgotten.
- [WATCH] The trend checker has still never produced a real verdict — 2 consecutive TIMEOUTs (2026-07-28). The wall-clock fix works exactly as designed (clean TIMEOUT, full trace, pipeline unharmed), but 600s is too tight for the grader to converge, so the checker's actual grading value is not being realised. Evidence from CI run 30341666361: the session took **6m18s before its first tool call**, then made 2 genuinely CORRECT corrections (t1, a John Oliver crypto video, on_topic False→True fit 5.7; t27, "finding bitcoin in 2009", False→True fit 7.8) and was cut off at 600s with grader iterations=0. So it is finding real misjudgements and being killed before it can finish reporting them — the value is there, the budget isn't. Two options, not yet chosen: (a) raise `TREND_CHECKER_BUDGET_SECONDS` to ~1200 (Monday worst case becomes ~1.5min sync + ~9.5min rebuild + ~21min checker ≈ 32min, still inside `timeout-minutes: 45`), or (b) cut `_CANDIDATE_CAP` from 30 to ~15 so a grade fits the current budget. (a) costs job time, (b) costs coverage. Needs a call.
- [WATCH] The trend checker's log file is never persisted from CI (pre-existing, found 2026-07-28). `_Log` now writes `docs/trend_checker_log/<stamp>.log` incrementally, but that path is on the runner's disk and is NOT in `_deploy_dashboard`'s `built` list, so it is discarded with the runner. CLAUDE.md presents that file as the framework-agnostic record of every grade and verdict; in CI the only real record is the Actions run log (which IS now complete, thanks to the per-line flush + PYTHONUNBUFFERED). The 4 `trend_checker_log/*.log` files on `gh-pages` are leftovers from the original branch seed, not from any scheduled run. Small fix: add the newest checker log to the deploy file list (or commit it) so the verdict trail survives without needing Actions-log access.
- [WATCH] `_stop_session` cannot always stop a timed-out Managed Agents session inside its 60s settle window (2026-07-28). Measured: a session interrupted on the normal path goes idle in ~5s, but one interrupted while mid-work ignored 6 interrupts over 60s and stayed `running` (CI run 30341666361); a separately-observed abandoned session took ~13 minutes to go idle on its own. Neither archive nor delete is permitted while `running`, so there is no force-stop. It does eventually stop, so the exposure is a few cents at $0.08/session-hour, and the code now logs the status plainly instead of swallowing a 400. Widening the settle window would just spend CI time waiting; left as-is deliberately.
- [BLOCKED] TrySpeed/Marketing-Agent's own daily-sync has NEVER had a green run — 0 of 7 (2026-07-22 to 2026-07-28). Discovered 2026-07-28. It is a SECOND scheduler, separate from Namzy100's: cron fires ~05:0x UTC (Namzy100's ~06:2x) and it deploys to `speed/gh-pages`. Every Google-Sheets-touching step fails identically with `Expecting value: line 1 column 1 (char 0)`: Adjust, Country Installs, Meta, Meta Creatives, Last Updated, and the creative Dashboard (which reads Sheets). The Supabase/Claude-only steps (creator, strategy, merchant, landing) succeed and deploy, which is why gh-pages looked healthy and hid this. Cause: `GOOGLE_SHEETS_CREDS_JSON` in TrySpeed's Actions secrets is empty or not valid JSON — the workflow writes it to `$RUNNER_TEMP/gsheets.json`, and `pipelines/sheets.py:68` only checks the file EXISTS, never that it parses, so an empty file sails past the guard and dies inside google-auth's `json.load`. Both repos have identical secret NAMES and identical workflow wiring for this step, and Namzy100's runs succeed, so the value is what differs. FIX (needs Jay / a TrySpeed admin — the assistant cannot read or set that secret): re-paste the service-account JSON into TrySpeed → Settings → Secrets → Actions → `GOOGLE_SHEETS_CREDS_JSON`. Worth hardening `sheets.py` to parse-check the creds file and fail with a clear message rather than a bare JSON error. Also: TrySpeed's Monday 2026-07-27 run hit the SAME 30-min timeout (cancelled at 30m21s) as Namzy100's — the 2026-07-28 checker/ordering fix must reach `speed/main` too, which needs the handover branch merged.
- [WATCH] Instagram reel-scraper TLS retry storm inside the weekly trend rebuild (real, lower priority, NOT part of the 2026-07-28 timeout fix). During the 2026-07-27 Monday run the `apify/instagram-reel-scraper` actor threw a wall of `SSL routines:ssl3_read_bytes:tlsv1 alert decode error` / `SSL alert number 50` failures against `i.instagram.com/api/v1/users/web_profile_info/`, each request retried 1-2 times (river_financial, muunwallet, breedlove22, moonpay, strike, grayscale, walletofsatoshi, altcoinbuzz, boxmining, swanbitcoin, unchainedcapital, voltage_cloud, lightspark). The rebuild still SUCCEEDED, so nothing is broken and no output is wrong — but the retries are a large share of why the trend rebuild takes ~11 min rather than the ~6 min CLAUDE.md claims, and that duration is what left the 30-min job timeout with no headroom. Deliberately NOT touched while fixing the timeout (changing scraper behaviour during an active fire is the riskier move); the timeout was raised to 45 min instead. A future pass should cut the wasted retries — likely by trimming the handle list to ones IG actually serves, or moving that fetch to the profile-scraper actor used by `creators/apify_instagram.py`. Evidence: Actions run 30244159173, log lines 666-732.
- [WATCH] Instagram description gap (honest, likely permanent for the legacy set): the 11 IG creators are Mimanshi spreadsheet imports — display names (not usernames), no stored handle, and there is NO IG creator fetcher (only the trend reel-scraper, which is content not profiles). So these 11 cannot get descriptions by re-fetch. The PERSISTENCE path is IG-ready (`database.py` persists `description` for any platform), so a future IG *creator* fetcher (username-mode profile actor) that returns a bio would just work — but it would need real usernames, which these legacy rows don't have. Not fixable for the existing 11 without manual username entry.

# Resolved (kept briefly for context, delete anytime)
# - Anthropic key: RE-FIXED + verified end-to-end 2026-07-24. Timeline: the 07-22 key
#   worked 07-22 (manual) and 07-23 (scheduled, both green) then went invalid again by
#   07-24 — a real 401 "API key is invalid" that failed 8 of the last 10 scheduled runs
#   (only the Claude-dependent steps: creative insight cards + strategy rebuild; Adjust,
#   Meta, and the data-dashboard deploys ran fine before the failure, which is why those
#   dashboards stayed fresh while strategy stalled at 07-23). New key swapped into .env +
#   the ANTHROPIC_API_KEY Actions secret on 07-24, org 1ddc103d-a7e2-468e-87af-58a47e7a2f70,
#   verified two ways: (1) real messages call -> HTTP 200; (2) manual workflow_dispatch
#   (run 30079123849) completed SUCCESS, 10/10 steps, zero 401s, Strategy dashboard AND
#   Creative insights (5 cards, claude-sonnet-4-6) both rebuilt successfully and deployed
#   live at 2026-07-24 08:31 UTC (origin/main f5f470d). Watch for another silent expiry.
# - Repo import: DONE — codebase merged into TrySpeed/Marketing-Agent main by complying with the require-linked-issue ruleset (issue #1984), no bypass needed.
